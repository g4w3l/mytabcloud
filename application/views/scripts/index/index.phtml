<?php  
    	
	foreach($this->messages as $msg) {
		$arr_msg = explode(MyTabCloud_Constants::MSG_SEPARATOR, $msg);
		echo '<div class="'.$arr_msg[0].' flashmessage">'.$arr_msg[1].'</div>';
	}
	
	if($this->user()) {
        echo "Connecté : " . $this->user() . "<br />";		
    } else {
        echo "Non connecté";
    }
            
?>